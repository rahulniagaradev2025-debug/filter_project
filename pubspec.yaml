name: filter_project
description: "Filter project based on Bluetooth"
publish_to: 'none'
version: 1.0.0+1

environment:
  sdk: ^3.11.1

dependencies:
  flutter:
    sdk: flutter
  cupertino_icons: ^1.0.8
  flutter_bloc: ^9.0.0
  get_it: ^8.0.0
  equatable: ^2.0.7
  dartz: ^0.10.1
  flutter_blue_plus: ^1.34.4
  permission_handler: ^11.3.1
  shared_preferences: ^2.5.1
  excel: ^4.0.5
  path_provider: ^2.1.2
  share_plus: ^7.2.2
  intl: ^0.20.2

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^6.0.0

flutter:
  uses-material-design: true
  assets:
    - assests/icons/
    - assests/images/

